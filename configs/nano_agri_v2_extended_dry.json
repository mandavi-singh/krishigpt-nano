{
  "run_name": "v2ext_dry",
  "corpus": "v2",
  "device": "cpu",
  "torch_threads": 10,
  "seed": 0,
  "seq_len": 128,
  "batch_size": 16,
  "epochs": 4,
  "max_steps": 4636,
  "lr": 6e-4,
  "min_lr": 6e-5,
  "warmup_steps": 200,
  "weight_decay": 0.1,
  "grad_clip": 1.0,
  "betas": [0.9, 0.95],
  "eps": 1e-8,
  "dropout": 0.0,
  "log_every": 5,
  "val_every": 15,
  "save_every": 100000,
  "warm_start": "checkpoints/krishigpt_nano_v2/best.pt",
  "update_warm_start": true,
  "extend_schedule": { "max_steps": 4636 },
  "total_batches": 30,
  "train_stats_only": true,
  "notes": "DRY RUN of the v2 continuation, same code path as the real run (warm-start from krishigpt_nano_v2/best.pt, update_warm_start bookkeeping, cosine extended to 4636 steps), capped at 30 training batches and stats-only checkpoints (no weights persisted). Disposable - never a resume source. The real run uses configs/nano_agri_v2_extended.json in a separate, clean directory."
}
