{
  "run_name": "krishigpt_v3_dry",
  "corpus": "v3",
  "device": "cpu",
  "torch_threads": 10,
  "seed": 0,
  "seq_len": 128,
  "batch_size": 16,
  "epochs": 4,
  "max_steps": 3840,
  "lr": 6e-4,
  "min_lr": 6e-5,
  "warmup_steps": 200,
  "weight_decay": 0.1,
  "grad_clip": 1.0,
  "betas": [0.9, 0.95],
  "eps": 1e-8,
  "dropout": 0.0,
  "log_every": 5,
  "val_every": 15,
  "save_every": 100000,
  "warm_start": "checkpoints/krishigpt_nano_v2_extended/best.pt",
  "total_batches": 30,
  "train_stats_only": true,
  "notes": "DRY RUN of the v3 warm start, same code path as the real run (warm-start load of krishigpt_nano_v2_extended/best.pt with fresh-schedule counters, cosine over 3,840 steps), capped at 30 training batches and stats-only checkpoints (final.pt keeps metadata only; run dir checkpoints/krishigpt_v3_dry is disposable and never a resume source). Gates: dataloading, resume/counter state, loss in-range, checkpoint writing, token accounting (30 x 2048 = 61,440)."
}
